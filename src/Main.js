import React from 'react';
import { SafeAreaView, ScrollView } from 'react-native';

export const Main = () => {
  return (
    <SafeAreaView>
      <ScrollView>
        {/* これから実装する */}
      </ScrollView>
    </SafeAreaView>
  )
}
